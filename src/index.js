import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';
import SeatReservationApp from './App';
import { createRoot } from 'react-dom';


ReactDOM.render(
  <React.StrictMode>
    <SeatReservationApp />
  </React.StrictMode>,
  document.getElementById('root')
  
);
